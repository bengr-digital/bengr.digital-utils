export const randomFunc = () => {
	return true;
};

/**
 *
 * @param {Number} number Parameter is number.
 * @returns {Number} Returns random number
 * @example
 * getId(12)  returns 12
 */
export const getId = (number: number) => {
	return number;
};
